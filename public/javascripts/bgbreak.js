window.addEventListener('load', (ev) => {
    // Array.from(document.querySelectorAll('.bg-break')).forEach((el) => {
    //     el.style.minHeight = String(window.innerHeight || document.documentElement.clientHeight) + 'px';
    // });
});
